
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geoquery2-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
       name: geoquery-pv
