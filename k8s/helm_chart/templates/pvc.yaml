
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geoquery-pvc
  namespace: {{.Release.Namespace}}
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
       name: geoquery-pv
