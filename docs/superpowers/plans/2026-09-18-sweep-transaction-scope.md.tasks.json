{
  "planPath": "docs/superpowers/plans/2026-09-18-sweep-transaction-scope.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: Split the sweep's per-request transaction into claim / work / finalize",
      "status": "completed",
      "description": "**Goal:** _manage_user_requests holds a requests row lock only for two short status transitions, never across _check_request_tasks or _build_output.\n\n**Files:**\n- Modify: backend/analytics/management/commands/manage_user_requests.py:116-196\n- Modify: backend/analytics/tests/test_manage_user_requests.py\n\n**Acceptance Criteria:**\n- [ ] A new _claim_request(request_id) helper claims a request in its own short transaction.atomic() block using select_for_update(skip_locked=True), filtered on status__in=(-1, 0), returning (claimed, original_status).\n- [ ] _check_request_tasks and _build_output are called outside any transaction.atomic() block.\n- [ ] The final status transition (0 when not ready, 1 when complete) happens in its own short transaction.atomic() block.\n- [ ] A request already at status=2 is not claimed by another sweep.\n- [ ] A request whose row is locked by another sweep's in-flight claim is skipped, not waited on (skip_locked=True).\n- [ ] dry_run performs no status writes at all (no status=2, no revert).\n- [ ] send_received_email still fires only when original status was -1; both _notify_user calls still happen outside any transaction.\n- [ ] Existing validation errors still set status=-2 via _request_error and skip the request.\n- [ ] Existing tests in test_manage_user_requests.py pass unmodified in intent.\n\n**Verify:** `sudo docker compose exec backend uv run python manage.py test analytics visualize -v 1` -> all pass except the known pre-existing unrelated failure (test_integrity_error_on_create_falls_back_to_get).\n\n**Steps:** See plan document Task 1, Steps 1-7 for full code (new SweepClaimTests class, _claim_request helper, rewritten loop body).\n\n```json:metadata\n{\"files\": [\"backend/analytics/management/commands/manage_user_requests.py\", \"backend/analytics/tests/test_manage_user_requests.py\"], \"verifyCommand\": \"sudo docker compose exec backend uv run python manage.py test analytics visualize -v 1\", \"acceptanceCriteria\": [\"_claim_request uses select_for_update(skip_locked=True) in its own short committed transaction\", \"_check_request_tasks and _build_output run outside any transaction\", \"final status transition is its own short transaction\", \"status=2 requests are not re-claimed\", \"row-locked requests are skipped not awaited\", \"dry_run writes no status at all\", \"notifications unchanged and still outside transactions\", \"existing sweep tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 2: Make _build_output atomic (build in a temp dir, then rename)",
      "status": "pending",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** Two sweeps building the same request can never corrupt each other's output, regardless of reaper timing.\n\n**Why before the reaper:** Task 1's claim fence stops a stale sweep *finalizing* a request but is not mutual exclusion over the output directory. Once the reaper can reset a still-running build, two sweeps can both be inside _build_output, which opens with shutil.rmtree(request_dir) -- one deletes the other's in-progress files, and the survivor emails a download link to a possibly-mixed zip.\n\n**Files:**\n- Modify: backend/analytics/management/commands/manage_user_requests.py (_build_output)\n- Modify: backend/analytics/tests/test_manage_user_requests.py\n\n**Acceptance Criteria:**\n- [ ] _build_output writes into a unique temp directory, then moves it into place as the final step.\n- [ ] Temp dir is a sibling of the final request_dir (same filesystem) so the move is a real rename, not a cross-device copy.\n- [ ] Final output path unchanged: <requests_dir>/<request_id>/<request_id>.zip, zip internal paths structured as before.\n- [ ] Two concurrent _build_output calls both complete and the surviving directory is internally consistent.\n- [ ] A crashed build leaves no partial request_dir, only an abandoned temp dir.\n- [ ] FullSubmissionToCompletionFlowTest passes unmodified, proving the output contract didn't change.\n\n**Verify:** `sudo docker compose exec backend uv run python manage.py test analytics visualize -v 1` -> all pass except the known pre-existing unrelated failure.\n\n**Steps:** See plan document Task 2, Steps 1-8. Step 1 covers the make_zipfile base_name trap (the zip filename derives from base_name, so building in a temp dir renames the archive unless base_name is passed explicitly). Step 6 is a mutation check.\n\n```json:metadata\n{\"files\": [\"backend/analytics/management/commands/manage_user_requests.py\", \"backend/analytics/tests/test_manage_user_requests.py\"], \"verifyCommand\": \"sudo docker compose exec backend uv run python manage.py test analytics visualize -v 1\", \"acceptanceCriteria\": [\"builds into a unique sibling temp dir then renames into place\", \"final zip path and internal structure unchanged\", \"concurrent builds cannot corrupt each other\", \"failed build leaves no partial request_dir\", \"FullSubmissionToCompletionFlowTest passes unmodified\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 3: Add the stale-claim reaper",
      "status": "pending",
      "blockedBy": [
        0,
        3
      ],
      "description": "**Goal:** A request stranded at status=2 by a crashed sweep is returned to status=0 and retried, instead of being invisible forever.\n\n**Files:**\n- Create: backend/analytics/management/commands/reset_stale_requests.py\n- Modify: backend/analytics/tasks/maintenance.py\n- Modify: backend/geoquery/settings.py (CELERY_BEAT_SCHEDULE entry)\n- Create: backend/analytics/tests/test_reset_stale_requests.py\n\n**Acceptance Criteria:**\n- [ ] _reset_stale_requests(minutes, dry_run=False) resets status=2 requests whose process_time is older than minutes to status=0.\n- [ ] Requests at status=2 with a recent process_time are left alone.\n- [ ] A request at status=2 with NULL process_time is also reaped (NULL < NOW() - INTERVAL is NULL, not true, so a bare comparison would strand it).\n- [ ] Requests at other statuses are never touched.\n- [ ] Reaped requests go to status=0, not -1 (avoids re-sending the received email).\n- [ ] A reset_stale_requests management command wraps it with --dry-run and --minutes.\n- [ ] A reset_stale_requests @shared_task reads STALE_TASK_MINUTES (default 30) and logs the count.\n- [ ] An hourly CELERY_BEAT_SCHEDULE entry runs it.\n\n**Verify:** `sudo docker compose exec backend uv run python manage.py test analytics.tests.test_reset_stale_requests -v 2` -> all tests pass.\n\n**Steps:** See plan document Task 3, Steps 1-9 for full code. Step 7 verifies the task actually registers with Celery (guards against the 0.43.1 silent-KeyError incident).\n\n```json:metadata\n{\"files\": [\"backend/analytics/management/commands/reset_stale_requests.py\", \"backend/analytics/tasks/maintenance.py\", \"backend/geoquery/settings.py\", \"backend/analytics/tests/test_reset_stale_requests.py\"], \"verifyCommand\": \"sudo docker compose exec backend uv run python manage.py test analytics.tests.test_reset_stale_requests -v 2\", \"acceptanceCriteria\": [\"stale status=2 claims reset to 0 not -1\", \"NULL process_time rows are also reaped\", \"fresh claims untouched\", \"other statuses never touched\", \"management command with --dry-run and --minutes\", \"shared_task reads STALE_TASK_MINUTES\", \"hourly beat entry added\", \"task confirmed registered with celery inspect\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 4: Concurrency regression test",
      "status": "pending",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** Prove with real concurrent connections that a second sweep skips a claimed request instead of blocking on it -- the actual property this plan exists to guarantee.\n\n**Files:**\n- Modify: backend/analytics/tests/test_manage_user_requests.py\n\n**Acceptance Criteria:**\n- [ ] A TransactionTestCase test holds a real row lock on a request from one connection and asserts a concurrent _claim_request returns claimed=False promptly rather than blocking.\n- [ ] The test completes well within its timeout, proving skip_locked is in effect (without it the second claim would block until the first transaction ends).\n\n**Verify:** `sudo docker compose exec backend uv run python manage.py test analytics.tests.test_manage_user_requests.ClaimContentionTest -v 2` -> passes.\n\n**Steps:** See plan document Task 3, Steps 1-4 for full code (ClaimContentionTest, threading/connection/transaction imports).\n\n```json:metadata\n{\"files\": [\"backend/analytics/tests/test_manage_user_requests.py\"], \"verifyCommand\": \"sudo docker compose exec backend uv run python manage.py test analytics.tests.test_manage_user_requests.ClaimContentionTest -v 2\", \"acceptanceCriteria\": [\"TransactionTestCase with two real connections proves skip_locked skips rather than blocks\", \"test completes promptly, not after the holding transaction ends\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-09-18T12:00:00Z"
}